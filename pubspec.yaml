name: payo_payment
description: PaYo – Smart Payment App (Dark Premium Theme)
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  device_preview: ^1.3.1
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0   # optional: untuk font Poppins via Google Fonts

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
